
  # Bank Portal

  This is a code bundle for Bank Portal. The original project is available at https://www.figma.com/design/glODokucCRs64s4eQqKYnd/Bank-Portal.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  